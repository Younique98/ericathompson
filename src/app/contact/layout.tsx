export const metadata = {
  title: "Contact",
  description:
    "Get in touch with Erica Thompson for technical audits, fractional CTO advisory, or team training.",
  alternates: { canonical: "/contact" },
};

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
